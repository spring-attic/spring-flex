version=1.0.1
release.type=integration
natural.name=spring-flex
project.name=Spring BlazeDS Integration
project.key=FLEX
ivy.cache.dir=${basedir}/../ivy-cache
integration.repo.dir=${basedir}/../integration-repo
ivy.local.repo.dir=${basedir}/../local-repo
javadoc.exclude.package.names=org/springframework/flex/samples/**
javadoc.max.memory=256M
#ivy.settings.file=${basedir}/../build-spring-flex/ivysettings.xml

# For when releasing
#release.type=release
#build.stamp=RELEASE

# For when releasing milestones
#release.type=milestone
#build.stamp=M1

# For development in trunk
#release.type=integration


