<beans:beans xmlns="http://www.springframework.org/schema/flex"
  xmlns:beans="http://www.springframework.org/schema/beans"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
              http://www.springframework.org/schema/flex http://www.springframework.org/schema/flex/spring-flex-1.0.xsd">
              
    <!-- expanded version -->
    <message-broker id="customId" configuration-manager="" services-config-path="/WEB-INF/flex/services-config.xml">
    	<config-processor ref="myCustomConfigProcessorBean"/>
    	<mapping pattern="/*"/>
    	<secured  authentication-manager="_authenticationManager" access-decision-manager="_accessManager" 
    		per-client-authentication="false">
	    	<secured-channel channel="my-secure-amf" access="ROLE_USER, ROLE_ADMIN"/>
	    	<secured-endpoint-path pattern="**/messagebroker/*" access="ROLE_ANONYMOUS"/>
		</secured>
		<!--  <remoting-adapter class="foo.bar.CustomAdapter"/> -->
    </message-broker>

    <remote-service message-broker="customId" ref="ping" service-id="pingRemote" exclude-methods="foo, bar" include-methods="baz"/>
	<remote-service message-broker="customId" ref="productService" channels="my-amf, my-secure-amf"/>

    <!-- minimal version -->
    <message-broker>
    	<secured/>
    </message-broker>

	<remote-service ref="ping"/>
	<remote-service ref="productService"/>

	<beans:bean id="foo" class="com.bar.MyClass">
    	<remote-service/>
	</beans:bean>
</beans:beans>